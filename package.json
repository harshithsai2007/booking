{
    "name": "luxstay-fullstack",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "seed": "cd server && npm run seed"
    }
}